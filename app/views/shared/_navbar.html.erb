<nav class="flex justify-between items-center bg-blue-400 text-blue-100 text-xl px-4 py-2">
  <!-- items-center垂直置中 -->
    <a href="/"> <!-- 連結回首頁 -->
      <i class="fa-brands fa-trello"></i> Task
    </a>

    <div>
      <% if user_signed_in? %>
        <span class="mr-2"><%= current_user.email %></span>
        <%= link_to "登出", destroy_user_session_path, method: 'delete', class: 'px-3 py-2 bg-blue-300 rounded-sm hover:bg-blue-200'  %>
        <!-- <%= link_to "登出", destroy_user_session_path, method: 'delete', class: 'user-button' %> -->
        <!-- destroy_user_session    DELETE   /users/sign_out(.:format)         devise/sessions#destroy -->
      <% else %>
        <%= link_to "登入", new_user_session_path, method: 'delete', class: 'px-3 py-2 bg-blue-300 rounded-sm hover:bg-blue-200' %>
        <!-- <%= link_to "登入", new_user_session_path, method: 'delete', class: 'user-button' %> -->
        <!-- new_user_session    GET    /users/sign_in(.:format)          devise/sessions#new -->
        <%= link_to "註冊", new_user_registration_path, method: 'delete', class: 'px-3 py-2 bg-blue-300 rounded-sm hover:bg-blue-200' %>
        <!-- <%= link_to "註冊", new_user_registration_path, method: 'delete', class: 'user-button' %> -->
        <!-- new_user_registration    GET    /users/sign_up(.:format)         devise/registrations#new -->
      <% end %>
    </div>
</nav>